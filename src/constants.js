// Page dimensions - Fixed size for all pages
export const PAGE_WIDTH = 794;
export const PAGE_HEIGHT = 1123;

// Page settings template
export const DEFAULT_PAGE_SETTINGS = {
  width: PAGE_WIDTH,
  height: PAGE_HEIGHT,
  background: {
    type: 'color',
    value: '#ffffff'
  }
}; 